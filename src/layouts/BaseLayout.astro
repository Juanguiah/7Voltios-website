---
import SEOHead from "../components/SEOHead.astro";
import "../styles/global.css";
import "../styles/shell.css";

export interface Props {
  title?: string;
  description?: string;
  socialTitle?: string;
  socialDescription?: string;
  canonical?: string | URL;
  image?: string | URL;
  imageAlt?: string;
  type?: "website" | "article" | "product";
  noindex?: boolean;
  bodyClass?: string;
  structuredData?: Record<string, unknown> | Record<string, unknown>[];
}

const {
  title = "7 VOLTIOS | Movilidad eléctrica y proyectos eléctricos",
  description = "Cargadores, instalación e infraestructura para movilidad eléctrica y desarrollo de proyectos eléctricos con respaldo de ingeniería en Colombia.",
  socialTitle,
  socialDescription,
  canonical,
  image,
  imageAlt,
  type,
  noindex,
  bodyClass,
  structuredData,
} = Astro.props;
---

<!doctype html>
<html lang="es-CO">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#031326" />

    <SEOHead
      {title}
      {description}
      {socialTitle}
      {socialDescription}
      {canonical}
      {image}
      {imageAlt}
      {type}
      {noindex}
      {structuredData}
    />

    <link
      rel="icon"
      type="image/png"
      sizes="16x16"
      href="/img/favicon-7v-16.png?v=20260727-master"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="32x32"
      href="/img/favicon-7v-32.png?v=20260727-master"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="48x48"
      href="/img/favicon-7v-48.png?v=20260727-master"
    />
    <link
      rel="apple-touch-icon"
      sizes="180x180"
      href="/img/favicon-7v-180.png?v=20260727-master"
    />
    <link rel="manifest" href="/site.webmanifest?v=20260920-orientacion" />

    <slot name="head" />
  </head>
  <body class={bodyClass}>
    <slot />

    <script>
      import "../scripts/main.js";
    </script>
  </body>
</html>
